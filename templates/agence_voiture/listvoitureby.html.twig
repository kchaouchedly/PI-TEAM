{% extends 'baseBack.html.twig' %}
{% block body %}

    <h1> list voiture </h1>
    </a>

    <table border="0" class="display table table-bordered" id="hidden-table-info">
        <tr>
            <td>id</td>
            <td>nbrplace</td>
            <td>type</td>
            <td>marque</td>
            <td> datedebut</td>
            <td> datefin</td>
            <td> prix</td>
            <td> disponibilite </td>
            <td> Image </td>
        </tr>
        {% for v in tabvoitureby %}
            <tr>
                <td>{{ v.id }}</td>
                <td>{{ v.nbrplace }}</td>
                <td>{{v.type }}</td>
                <td>{{ v.marque }}</td>
                <td>{{ v.datedebut|date('d/m/y') }}</td>
                <td>{{ v.datefin|date('d/m/y') }}</td>
                <td>{{ v.prix }}</td>
                <td>{{ v.disponibilite }}</td>
                <td>
                    <img src="../../public/uploads/images/products/{{ v.image }}"  width="150"></td>

                <td> <a href="{{ path('updatevoiture',{'id':v.id}) }}" class="btn btn-primary btn-xs">
                        <i class="fa fa-pencil">

                        </i>
                    </a>
                </td>

                <td> <a href="{{ path('deletevoiture',{'id':v.id}) }}" class="btn btn-danger btn-xs">
                        <i class="fa fa-trash">

                        </i>
                    </a>
                </td>
            </tr>

        {% endfor %}
    </table>








{% endblock %}
